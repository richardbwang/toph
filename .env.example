# Postgres connection string (Neon / Supabase / local). Prisma reads this.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
# Optional: enables the AI extraction step on the /record page.
ANTHROPIC_API_KEY=""
